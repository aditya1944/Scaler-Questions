#include<iostream>
#include<map>
#include<unordered_map>
int main(){
    std::unordered_multimap<int, int> hash;
    hash.insert(std::pair<int, int>(1,2));
    hash.insert(std::pair<int,int>(1,3));
    if()
}